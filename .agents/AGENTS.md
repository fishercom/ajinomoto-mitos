# Reglas del Agente

- **Control de Versiones (Git)**: Queda estrictamente prohibido realizar acciones de control de versiones (tales como `git add`, `git commit` o `git push`) sin obtener previamente el consentimiento y la aprobación explícita del usuario.
